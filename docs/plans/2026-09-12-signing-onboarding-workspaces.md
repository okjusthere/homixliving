# Portal 文件签署与管理员入职工作台实施范围

日期：2026-09-12

状态：实施中。Portal 与 eSign bridge 的代码、数据库回归、原生 API/回调和模板导入已完成首轮验证；真实双人最终签署、完整上线切换及旧引擎退役仍在进行。
主计划：相邻 eSign 仓库的 `docs/DOCUMENSO_PORTAL_PLAN.md`；本文件记录 Portal 侧交付与回归边界。

## 用户已确定的入口

| 场景 | Portal 入口 | 目标行为 |
| --- | --- | --- |
| 经纪人入职合同 | 登录后自动识别并恢复 `/pending` 当前入职步骤 | 资料确认后准备合同，本人继续签署和付款；不去通用工作台找模板，不重复建合同或发信 |
| 买家/卖家 package 与个性化文件 | 工作台固定“文件签署”入口，建议 `/signing` | 一处选包、预填、确认、发送、查看状态、继续原生编辑和下载 |
| 管理员入职处理 | `/admin/agents?view=onboarding` → 处理入职 | 资料、合同、收款、实际权限独立处理，按未完成事项跟进 |

Documenso 负责所有电子签署与原生编辑。Portal 管理业务身份、线下/历史合同核验、收款、正式开通及有限权限例外。电子签署事实不能由管理员例外或前端回跳参数伪造。

当前一两份 native 待签任务不迁移，也不需要双引擎兼容。“既有人员历史合同核验”作为日后需要的业务能力，仍必须建设；它与迁移 native 草稿不同。

## 文件签署工作台

- 工作台导航新增“文件签署”，包含买家包、卖家包、上传个性化文件三个入口。
- 统一列表：待我处理、草稿、待他人签署、已完成、异常/过期；按客户、房产、文件名搜索。
- 列表每行有文件/包名、业务关联、签署进度、更新时间和下一步。详情含原件、签署人、事件和完成文件。
- 包先选择公司发布的实际文件组合，资料填一次，预览收件人及文件清单后确认发送；不自行杜撰法律文件组合。
- 编辑使用 Documenso 原生界面，返回 Portal 时回到该任务并保留筛选条件；不能仅跳至上游首页。
- 经纪人只看自己的客户文档及明确授权文件。Documenso 原生权限必须同样成立；HR 文件不会因共享团队模板而被全员访问。
- 个人入职合同在入职/个人资料中查看，不混入客户包主操作。管理员包配置进入已有管理员系统。

## 管理员工作台交付

### 以实际事项组织待办

默认统计未完成事项对应的人，而不是只查 pending 账号。已开通但待公司会签、例外待补签、线下合同待核验、收款待匹配仍要出现。人员数与事项数分别标注，避免重复计人。

列表直接显示卡点、下一责任人和动作；详情有资料、合同、收款、权限四区及共用时间线。暂不处理/关闭本次入职需要原因并可恢复，不删除或抹掉实际合同、收款和授权记录。

### 四条办理路径

| 路径 | 管理动作 | 结果 |
| --- | --- | --- |
| 标准电子入职 | 继续/提醒签署、检查状态、核验实际收款、按条件审批 | 线上本人签署后 Stripe 付款自动开通；线下核验收款后管理员审批 |
| 纸质签署 | 上传已签文件、登记公司/用途/版本/日期/角色，核验或退回 | 标明“线下合同已核验”；满足对应要求，其他缺项继续待办 |
| 既有公司经纪人 | 核验历史合同、身份与适用条款、财务适用情况，专用开通 | 不重复新入职签约或自动收一次新入职费；不是伪造已签/已付款 |
| 未签，先用部分功能 | 理由、白名单能力、补签期限和责任人，授予有限权限 | 显示“例外开放 · 待补齐”；到期/撤销生效，实际合同和付款状态保持真实 |

纸质文件上传不等于核验通过。已核验文件替换必须新建版本再核验，核验可撤销并留痕。本人签完但公司未签仍保留会签待办。对不再使用的线上邀请安排明确取消动作及失败跟踪，避免继续重复催签。

### 先记录实际收款，再判断开通资格

本人未签、资料未全或团队条款未完成，不妨碍管理员登记真实收到的金额、方式、日期和凭据。项目/金额未确定时保留为“已收款 · 待匹配/核对”。

收款、匹配应付、套餐/奖励结算、账号开通分开处理。不得为了登记收到的钱就提前开通、开始套餐或重复产生奖励。复用现有账务规则，加入同一凭据、重复请求及线上/线下并发到款的防重。

### 例外权限是真正的有限权限

首期建议白名单为完善本人资料、培训学习、普通公司资源阅读，叠加原有资源可见权限；管理员逐项选择并设置截止时间。它不自动包含客户合同发送、财务或管理他人的能力。

例外作为独立授权记录，不将账号写成完整 active。统一计算 `effectiveAccess`；中间层、页面和 API 协同，不能一边允许培训、一边因非 active 被送回 `/pending`，也不能直接放开所有 API。

接口读取最新授权和服务端时间，因此旧 JWT 不能越过到期/撤销。后台只负责提醒与待办。完成正规入职后结束例外并取得完整权限，旧例外到期不能收回正规权限。

## 当前基线与具体改动点

基线：Portal `a02c539`。截图记录的是较早的分析，不能将其中已修复的问题再次当成现状。

| 文件 / 模块 | 当前状况与计划 |
| --- | --- |
| `src/components/nav.tsx` | 已有工作台分组；加入文件签署入口及正确的有限权限菜单 |
| `src/app/pending` | 现有入职界面；自动恢复当前步骤，改为 Documenso，接受已核验线下/历史合同，正确显示有限开放状态 |
| 新 `src/app/signing` | 文件签署列表、详情和发起入口，复用 Documenso 编辑与签署 |
| `src/app/agents/agents-client.tsx` | 已有入职处理和会签待办；拓展为所有未完成事项及新增筛选、明确人数/事项数 |
| `src/components/admin/onboarding-panel.tsx` | 已有可见步骤、下一动作与审批条件；扩展四区操作及线下/历史/例外表单 |
| `src/lib/onboarding-workflow.ts` | 当前 `canRecordPayment` 仍依赖已签；拆分合同要求、收款事实、开通资格、有效权限与待办计算 |
| `src/app/api/onboarding/payments/offline/route.ts` | 当前要求已签且直接套餐结算；调整为真实收款登记及应付匹配，审查账务副作用 |
| `src/app/api/agents/[id]/approve/route.ts` | 保留已有签约/付款及并发保护；读取多来源合同要求，明确标准与既有人员开通路径 |
| `src/app/api/agents/route.ts` | 普通编辑拒绝 pending → active 已修复，必须回归；继续检查创建、导入和恢复路径 |
| `src/auth.config.ts`、服务端权限 guards、session | 当前非 active 默认被拒绝；改为支持有限能力且不扩大其他 API 权限，处理即时撤销与到期 |
| `src/lib/esign.ts` 及签署同步模块 | 适配唯一 Documenso 的 task/recipient/文件；真实回调、幂等与补偿，保持业务动作授权 |
| `src/db/schema.ts`、迁移、审计、现有财务模块 | 补线下合同版本和核验、既有人员认定、收款匹配、例外授权等最小数据；复用原账本 |
| 当前 `src/app/api/documents/upload/route.ts` | 只允许 active 且绑定交易，不能直接用来上传待入职者的 HR 合同；复用存储底层但另做 HR 权限和关联 |

不重复改已收敛的培训、管理员权限、生日/纪念日入口；保持这些功能、邮箱账号关联及独立 Email Service 的现状。

## 实施顺序

1. 与 eSign Phase 0 同步确定真实 Documenso 身份/编辑/回程，以及 Portal 多来源合同、收款、开通和例外权限的数据与判断模型。
2. 完成数据库和服务端能力，再接四区管理员界面；线下核验、收款、既有人员和有限权限是同轮明确交付，不留作泛泛后续优化。
3. 将登录自动入职换到 Documenso，回归线上自动开通和线下审批，验证已开通但待公司签署仍留待办。
4. 交付独立文件签署工作台及买家、卖家、个性化文件流程。
5. 三条 Documenso 流程和管理员特殊路径验收通过后，完成 native 代码与服务退役。

## 验收用例

- 登录/刷新/换设备回到同一入职任务；已核验线下合同不强制重新电子签署；已开通但待会签不重新进入新入职。
- 未签先收到线下款可登记；资料未定保留待匹配，不错误开通和结算；重复/并发收款不重复入账。
- 纸质合同上传、核验、退回、撤销和替换可追踪；线下核验不改写 Documenso 的电子签署事实。
- 历史合同核验后通过既有人员路径开通，收费适用可解释，不重复新入职费；缺项仍有待办。
- 例外只允许指定页面与 API；到期/撤销在旧会话中立即阻止，补齐后正规权限不受旧例外影响。
- 正常编辑、导入/创建、恢复账号、签署和付款回调都不能绕过专用开通/授权判断。
- 管理员可找到已开通但仍未办完的人，人数与事项数正确，暂缓可恢复。
- 文件签署工作台原生编辑返回同一任务；客户共同签署人与多文件字段正确，文档及 HR 权限不越界。
- 两个工作台在桌面和移动端可用；新增页面遵循现有双语约定。

以上为实施前规划。实际执行状态见下方实施记录及两仓库的验收报告。


## 2026-09-12 实施记录

- 两家公司入职及 Team Leader 公司会签人已由用户确定：**Si Zhang，hr@homixny.com**。从核实过的 Documenso 身份取得姓名与邮箱，不把 Portal 管理员显示名当作签名身份。
- 新接口只调用 `esign/apps/bridge` 的 Documenso v2.18.0 API；包配置不再依靠旧模板环境变量。
- 已建设 `/signing`、`/admin/signing`、登录入职恢复、Team Leader、线下/历史合同核验、真实收款登记及匹配、有限权限与按事项统计的管理员队列。
- 本机真实 Documenso 已验证双文件双签署人建包、原生字段保存后恢复、精确编辑入口、A/B/HR 原生隔离、真实 native webhook、bridge 到 Portal 的持久化回调重试。
- 11 个已批准入职/Team Leader 包全部导入真实测试实例并校验字段、角色及 PDF 哈希。多页同名字段通过角色、页码和坐标定位。未发布任何自行杜撰的买卖法律文件。
- Portal 浏览器已验证买家包预览、创建草稿、详情、原生编辑；390px 手机布局及管理员四区面板继续验收。数据库回归覆盖重复收款、历史核验、例外权限、开通独立性及恢复失败重试。
- 已核验完整纸质合同也可满足 Team Leader 的入职资格；电子状态不会被改成“已签”。申请人仍需完成单独 Team Leader 合同。
- 新生产平台在 Central US 部署中；Azure 订阅不允许在 East US/East US 2 新建 PostgreSQL。使用独立私网与数据库，原生产流量尚未切换。

仍需完成：最终合成签署与封存下载证明、其余原生场景验收、生产切换验证及 native 代码/资源退役。生产 Documenso 身份、私网数据库、服务证书及 SMTP 认证配置已经完成。最终合成签署按钮仍在等待之前发出的用户确认；不能将模板导入或模拟签署测试当成已经完成电子签署验收。


## 生产候选与浏览器补充验收

- 新生产 Documenso 与 bridge 均健康；两家公司绑定 Si Zhang / hr@homixny.com，11 个批准的空白 HR 包已发布。专用 SMTP 已通过 TLS/认证检查，未向真实收件人发送测试邮件。服务证书为自签完整性证书，不是个人或 AATL 证书。
- Supabase 已应用新增迁移；五个新表启用 RLS，匿名和普通客户端无直接读写授权。旧业务数据未被改写。
- Vercel 首个候选已 Ready，采用 `--prod --skip-domain`，官网域名尚未切换。最终本地 UI 修复后已重新部署，候选 `dpl_CVLQWC8ibLvbPMxscv2rnYxUCa6t` 已 Ready；官网仍未切换。
- 手机端实测关闭原生签署页、刷新 `/pending` 再继续，会返回同一原生收件人任务。已提交资料默认收起，继续签署入口优先显示。
- 纸质合同浏览器实测使用独立本地 S3 模拟存储：上传待核验、资料缺失拒绝、核验通过后保留公司待签和账号 pending；下载字节 SHA-256 与原件相同，其他经纪人返回 404。
- 有限工作台显示仅培训入口。实际会话可访问培训，资料库和客户签署被拒绝；通过界面撤销后，同一旧 JWT 立即失去培训 API 权限。
- 已修正纸质签署日期显示、上传审计说明和中文办理错误提示；会话加载期间不再闪现完整业务导航。
- eSign 完整 `pnpm verify` 已通过：60 个测试、类型检查、lint、构建；Portal 完整 npm 测试此前已通过，最新改动的针对性回归与最终构建也已通过。
- 运行配置、逐人连接 Documenso 账号和故障处理见 eSign 的 `docs/DEPLOYMENT.md`、`docs/SIGNING_OPERATIONS.md`。客户编辑账号需要逐人连接，不能共用 HR 账号；买卖包正式法律文件仍待公司提供。

- 候选验收发现并修复了签署回调被登录中间层拦截的问题：仅放行准确的 POST 回调地址，仍由 HMAC 校验认证；真实本地 Portal HTTP 已验证原生状态同步、持久化与重放防重。修复提交 `950cf19`，新候选 `dpl_DLX4Uq9Yd4uMmYuwJV5dcGEjHV16` 已 Ready，生产 HMAC 认证边界验证通过，官网仍未切换。
